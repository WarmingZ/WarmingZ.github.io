---
title: Бази даних для програмістів - Частина 2🗂️
layout: post
subtitle: Розглядаємо прості та агрегатні функції
date: '2021-12-08 13:10:39'
background: "/img/AmAZUHZ.webp"
categories:
lang: ua
lang-ref: sql2
image: /img/posts/Database2/DB.png
description: Розглядаємо прості та агрегатні функції
---
В минулому пості я описував роботу простих серверних запитів до СУБД. Також, було описано мінімальний набір теорії для розуміння, з чим ми маємо справу. Розглянули прості SELECT запити, де ми фільтрували дані за допомогою WHERE. В цьому пості я хотів би продемонструвати використання простих та агрегованих функцій. Можливо зараз вам здається що все дуже легко, але так воно і є. Щоправда ми ще не вносили ніяких змін в наші таблиці та не створювали нові, але це поки що😊. 
<center>
<img src="/img/posts/Database2/DB.png" alt="Network" class="responsive" style="width: 65%; height: auto;">
</center>

***

### Використання простих функцій

В першу чергу хотілось би сказати що в SQL багато вбудованих функцій для виконання різних завдань. Я б хотів продемонструвати самі найчастіше використовувані функції. Також, якщо вам необхідно написати який специфічний запит і вам не вистачає функцій, які будуть розглянуті в цій статті, ви завжди можете відвідати офіційну документацію.
Оскільки, можна демонструвати дуже багато прикладів, я обрав лише самі цікаві на мою думку, для того, щоб рухатись швидше і не перевантажувати зайвою інформацією. Можливо в мне поки що не виходить писати коротко і локанічно, але вчуся🙄.

#### Функція `IF`⚙️

Отже, в таблиці `discussion_group` ми маємо комірку `approve_required`, в якій зберігається підтвердження користувача про реєстрацію в вигляді нулів та одиниць. І нам необхідно вивести користувачів. Для цього ми будемо використовувати оператор `if`.
{% highlight sql %}
select if(approve_required, 'required', ''), 
approve_required from discussion_group;
{% endhighlight %}
Ось такий результат ми отримали:

 | if(approve_required, 'required', '') | approve_required from |
 |:------------------------------------:|-----------------------|
 | required                             | 1                     |
 |                                      | 0                     |
 | required                             | 1                     |
 | required                             | 1                     |
 |                                      | 0                     |
 {: .table .table-responsive }

 Ми бачимо, що в назві колонки виводиться наша функція. Ми це можемо змінити до нормального вигляду за допомогою оператор `as`. 

 Також, в наступному прикладні ми перейдемо в колонку `disscusion_group`, де будемо визначати старі та нові групи. Запит буде виглядати так: <br>

{% highlight sql %}
 select group_id as ID , creation_time as CreationData ,
  if(creation_time > '2020-01-01', 'new', 'old')
  as Type from discussion_group;
{% endhighlight %}

Якщо дата створення більша ніж `2020.01.01` це нові, в протилежному випадку — старі. Ось результат:


 | ID     |     CreationData    | Type |
 |--------|:-------------------:|:----:|
 | 570779 | 2018-12-21 00:39:04 |  old |
 | 570890 | 2019-12-25 00:40:09 |  old |
 | 570853 | 2019-12-29 00:39:04 |  old |
 | 570863 | 2020-05-17 00:39:05 |  new |
 | 570922 | 2021-01-13 00:39:04 |  new |
 | 570888 | 2021-03-02 00:39:04 |  new |
 {: .table .table-responsive}
Як видно по запиту, ми перейменували наші комірки до зручного вигляду.

#### Функція `COALESCE`⚙️

 У мові SQL є такі функції, як `COALESCE` та `ISNULL`, які перевіряють вхідні параметри `NULL`.

 Отримуємо повідомлення з таблиці `user_private_message`, які були прочитані після певної дати. Але не у всіх повідомлень є дата прочитання, то ми будемо використовувати дату посилання для ще не прочитаних повідомлень і будемо використовувати цю дату в умові. Тут ми можемо використати функцію `coalesce` в умовах `where`.

{% highlight sql %}
 Select message_id, user_from_id,
  user_to_id from user_private_message
  where coalesce(read_time, send_time) > '2020-10-20';
{% endhighlight %}
Ось який результати ми отримали:

  | message_id | user_from_id | user_to_id |
  |:-------:|:----:|:-----:|
  | 6521171 | 8425 |  7838 |
  | 6521181 | 8144 |  8328 |
  | 6521186 | 7614 |  7720 |
  | 6521190 | 7960 |  7817 |
  | 6521200 | 7683 |  8163 |
  | 6521207 | 7490 |  8533 |
  {: .table .table-responsive}
Думаю тут все зрозуміло. Давайте перейдемо до функцій CONCAT.

#### Функція `CONCAT`⚙️

 Дана функція допомагає склеювати стрічки. Для прикладу ми можемо склеїти стрічку `firstname` і `lastname` - створивши такий запит: 
{% highlight sql %}
 select concat(first_name, ' ', last_name) as Name from user; 
{% endhighlight %}
І отримати такий результат:

  |  Name  |
  |:------:|
  | Carla Mariet |
  | Sophy Brags  |
  | Ivan Soloviov|
  | Doug Anne marie |
  {: .table .table-responsive}

Думаю тут також все зрозуміло і ми можемо переходити до наших агрегатних функцій.

***

## Агрегатні функції SQL

Насамперед агрегатні функції SQL діють щодо значень стовпця з метою отримання єдиного результуючого значення.
Розглянемо агрегатні функції на прикладі `COUNT`.

#### Функція `COUNT`🔩

В цьому прикладі давайте  підрахуємо, скільки ми маємо надісланих повідомлень та скільки з них було прочитано. Зробимо ми це за допомогою такого запиту:
{% highlight sql %}
 select count(*) from user_private_message 
  where read_time is not null ;
{% endhighlight %}
Тут ми підрахували скільки надісланих та прочитаних повідомлень. Тобто, ми рахували лише ті колонки, в яких відсутній `null`, в таблиці `user_private_message`. Спочатку надсилаємо стрічку `from user_private_message` і потім фільтруємо її (`where read_time is not null`) та надсилаємо в наш `count(*)`. 
В цьому випадку коли ми використовуємо зірочку ми вибираємо всі стрічки та завантажуємо їх в пам’ять.
Тепер якщо ми будемо передавати замість зірочки, параметр, то запит буде виглядати так:
{% highlight sql %}
 select count(read_time) from user_private_message;
{% endhighlight %}

Тобто ми можемо оцінити, на скільки наш запит, став компактнішим. <br>
💡**Також, варто запам'ятати! Багато програмістів використовують таку оптимізаційну "фішку", передаючи в `count(1)` одиницю. Тобто, вибираючи з таблиці, ми не використовуємо проміжну пам’ять, просто для кожної стрічки ми виконуємо функцію `count(1)`, яка взагалі не потребує ніяких значень ( вона просто додає +1 до деякого агрегатного значення ). Таким чином, ми не користуємося проміжною пам'яттю для зберігання значення, які ми навіть не збираємось вертати з СУБД.**

Ще існують такі функції як: `Average`, `Min`, `Max`, `Sum`. Дані функції я не буду описувати, оскільки з їх назви все зрозуміло. Ви з легкістю зможете знайти їх в інтернеті. Перепрошую за це, але якщо розписувати кожну функцію ми ніколи не перейдемо до самих цікавих і складних функцій😉

#### Функція `CAST`🔩

І на останок розглянемо просто цікаву функцію `CAST`. Дана функція дозволяє перетворити вираз одного типу даних в інший і вона не являється агрегатною. Натисни на мене [`CAST`](https://docs.microsoft.com/ru-ru/sql/t-sql/functions/cast-and-convert-transact-sql?view=sql-server-ver15), якщо хочеш детальніше дізнатися про функцію та аргументи, які вона може приймати.

В багатьох старих базах даних, рік писали лише двома цифрами. За допомогою касту ми можемо перетворити ті дані в новий формат.
{% highlight sql %}
 mysql> select cast("21"as year);
{% endhighlight %} 
І отримати таку відповідь на наш запит:

  |  cast("21"as year)  |
  |:-------------------:|
  |        2021         |
  {: .table .table-responsive}

Зараз, ми розглянули лише одну агретану функцію, яка була на мій погляд досить цікавою. Якщо ви бажаєте дізнатися про них більше, перейдіть по цьому [посиланню](https://www.sqlservertutorial.net/sql-server-aggregate-functions/).

***

Отже, можливо цей пост здався вам трішки заплутаним, але я раджу вам самостійно відтворювати всі запити, які були наведені в цьому пості. Це допоможе вам краще зрозуміти роботу цих запитів. Експерементуйте та користуйтесь документацією, але при цьому, будьте обережні. Оскільки, якщо ви встановили SQL Server по моєму прикладу ( [з минулої статті](https://www.soloviov.fun/sql/tech/translated/SQL-for-the-programmer-ua/) ), ви можете зламати сервер, так як ми працюємо як `root` користувач, маючи необмежені права доступу на сервері. Побачимось в наступному пості👋😉. 